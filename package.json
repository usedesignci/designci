{
  "name": "designci-workspace",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "tsc -b --clean",
    "test:action": "node --test 'action/*.test.mjs' 'tests/*.test.mjs'"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.136.0",
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.2",
    "vitest": "^2.1.8"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "license": "Apache-2.0"
}
